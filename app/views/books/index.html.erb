 <h1>Books</h1>
  <table>
    <thead>
     <tr>
      <th>Title</th>
      <th>Body</th>
      <td colspan=3></td>
     </tr>
    </thead>
    
    <tbody>
     <% @books.each do |book| %>
      <tr>
       <td><%= book.Title %></td>
       <td><%= book.Body %></td>
       <td><%= link_to "Show", book_path(book.id) %></td>
       <td><%= link_to "Edit", edit_book_path(book.id) %></td>
       <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %></td>
     <% end %>
    </tbody>
  </table>

<h1>New book</h1>
<% if @book.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    
    <%= pluralize(@book.errors.count, "error") %> prohibited this account from being saved:</h2>

    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<!-- form_for部分 -->
<%= form_for(@book, url: '/books') do |f| %>
 
    <h4>Title</h4>
    <%= f.text_field :Title %>
 
    <h4>Body</h4>
    <%= f.text_area :Body %>
 
    <%= f.submit 'Create Book' %>

<% end %>